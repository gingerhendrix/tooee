# Review notes

An embedded patch:

```diff
diff --git a/src/greet.ts b/src/greet.ts
index 1111111..2222222 100644
--- a/src/greet.ts
+++ b/src/greet.ts
@@ -1,3 +1,4 @@
 const greeting = "hello";
-const target = "world";
+const target = "terminal";
+const punctuation = "!";
 export { greeting };
```

And a fence that only looks like one:

```diff
- drop the old plan
+ adopt the new plan
```

End of notes.
