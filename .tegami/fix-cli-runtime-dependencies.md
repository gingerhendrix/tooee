---
packages:
  "@tooee/cli": patch
---

## Include the terminal runtime with the CLI

The Tooee CLI now installs OpenTUI and React as runtime dependencies. Global and one-off installs no longer need the user to supply the CLI's rendering runtime.
