---
packages:
  "@tooee/cli": patch
---

## Schedule removal of the table command alias

The deprecated `tooee table` command now reports that it will be removed in 0.8.0. Use `tooee view --renderer table` instead.
